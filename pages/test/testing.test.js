require("chai");

var assert = require('assert');

describe('Carrito works?', () => {
  describe('cart functionalities', () => {

    it('adding to cart', () => {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });

  });
});